import 'package:flutter/material.dart';

final kDartTheme = ThemeData.dark().copyWith(
  appBarTheme: AppBarTheme(
    color: Colors.brown
  ),
);

final kLightTheme = ThemeData.light().copyWith(
  appBarTheme: AppBarTheme(
      color: Colors.blue
  ),
);